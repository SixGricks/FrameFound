"""AI provider interfaces and local implementations (ADR-0008 territory).

Every capability is behind a small interface so models are swappable and the
application never hard-codes one vendor or architecture.
"""
